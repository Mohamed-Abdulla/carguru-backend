1. setup a node app with type script, express and postgresql with docker compose, use module based folder structure, use .env, make it prod grade, use winston, add middle ware, types, models  first run in port 3003.  use pg pooling. as well
global error handleer, express validator, helmet, cors, rate limiting, keep all .env relatged values in env config file

2. App logic havce a core recommendation engine service (this is core), recommendation service, routes for cars, session , recommondation controller, seed data js file,

api route: /cars, /cars:id
session persistance,:
/sessions = post
/sessions/:id = get
/sessions/:id/ = patch

also recommondatin routes, 

analysitcs, popular, stats, lets 